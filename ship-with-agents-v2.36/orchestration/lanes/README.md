# Lanes Runtime

This directory holds lane-local brain capsules.

Use one subdirectory per meaningful lane:

- `lanes/<stable-lane>/STATE.md`

Each capsule should stay small and answer:

- who this lane is
- what it owns
- where its inbox is
- which workstream it belongs to
- what should happen next

This is lane-local runtime truth, not a copy of full project strategy.
