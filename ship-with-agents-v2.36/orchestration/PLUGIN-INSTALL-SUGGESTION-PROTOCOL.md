# Plugin Install Suggestion Protocol

Use this when a plugin is not verified installed but would materially improve
the current task.

## Required Wording

Say all of these clearly:

- whether the plugin is already installed, unverified, or only marketplace-available
- why it helps this task specifically
- what the fallback is if the buyer skips it

## Final Rule

An install suggestion should reduce confusion, not create a fake dependency.
