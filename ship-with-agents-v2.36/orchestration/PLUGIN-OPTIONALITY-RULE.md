# Plugin Optionality Rule

Use this when a plugin would help, but the workflow should still remain usable
without it.

## Core Truth

The package should be plugin-aware by default and plugin-dependent only when
the task truly requires it.

## Final Rule

Plugins should usually be a force multiplier, not a hidden prerequisite.
