Use `HEAD-DECISION-RUBRIC.md`, `MANAGER-SUPER-AUDIT-RUBRIC.md`, and
`TOP-CHAIN-ANTI-PATTERNS.md` when a head or manager turn will materially shape
the rest of the workflow.

Do not let top-chain lanes:

- route by habit instead of value and sequencing
- pass through weak super packets without real challenge
- omit next-owner and bridge truth on meaningful handoffs
- treat a clean build report as equivalent to a clean closeout state
