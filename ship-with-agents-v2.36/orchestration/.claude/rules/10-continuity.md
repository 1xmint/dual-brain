# Continuity Rule

- Runtime files outrank stale chat memory.
- Use the update bus and inbox before assuming current workflow rules.
- Use checkpoints for current execution truth.
- Use closeouts for finish-state truth.
- If the system needs to remember something across sessions, put it in the
  smallest honest file instead of restating it every turn.
- Do not call a handoff complete if active pickup still matters now.
