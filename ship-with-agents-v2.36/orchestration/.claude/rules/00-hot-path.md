# Hot Path Rule

- Keep the always-loaded guidance small and behavioral.
- Use `AGENTS.md`, `CLAUDE.md`, and the current role file as the default hot
  path.
- Read detailed gate or reference docs only when the task actually needs them.
- If a repeated workflow has become ritual, prefer a project command over more
  prompt prose.
- Accept plain buyer language first; do not force internal jargon.
